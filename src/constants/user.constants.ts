export enum USER_TYPE {
  USER_ADMIN = 9,
  USER_REGULAR = 1,
  USER_STUDENT = 0,
}

export enum PF {
  PF_MAJOR = 'MAJOR',
  PF_MINOR = 'MINOR',
}

export const STATUS = {
  ENABLED: true,
  DISABLED: false,
}
